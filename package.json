{
  "name": "agent-workbench-core",
  "version": "0.3.1",
  "description": "A local-first, harness-neutral workspace core for persistent agent work context.",
  "type": "module",
  "bin": {
    "awb": "./bin/awb.js"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check bin/awb.js && node --check core/cli.js && node --check core/core.js && node --check core/profile.js && node --check core/providers.js && node --check core/schema.js && node --check core/profile-styles.js && node --check core/templates.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED",
  "private": true
}
